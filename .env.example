# ===================
# Required
# ===================

# Your wallet private key (without 0x prefix is also OK)
PRIVATE_KEY=your_private_key_here

# Legacy: UniversalSwap contract address (required by config, but not used - we use official routers)
UNIVERSAL_SWAP_ADDRESS=0x0000000000000000000000000000000000000000

# ===================
# Optional
# ===================

# RPC endpoint (default: BSC public dataseed)
RPC_URL=https://bsc-dataseed.binance.org/
# MEV-protected RPC (recommended for production)
# RPC_URL=https://rpc.48.club

# Slippage tolerance in % (default: 1)
# Range: 0.01 - 99.99, max 2 decimal places
SLIPPAGE=1

# Transaction deadline in seconds (default: 30)
DEADLINE_SECONDS=30

# Minimum pool liquidity in USD (default: 1000)
# Pools below this threshold are filtered out
MIN_LIQUIDITY_USD=1000
